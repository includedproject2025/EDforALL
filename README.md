Instalar:
npm install express mysql2 bcrypt cors multer 
npm install express-rate-limit


Inicializar Server: 
node index.js


Na conexão do MySQL (Adicionar .env):
APP_PORT=3001
DB_HOST=localhost
DB_PORT=3000
DB_USER= --> adicione seu user
DB_PASSWORD=--> adicione sua senha
DB_NAME=edequity
